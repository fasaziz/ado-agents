kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ${ProjectName}-yarn-cache-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: azurefile-csi-premium
